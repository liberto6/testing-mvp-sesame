# Verba Voice Assistant Configuration
# Copy this file to .env and configure your settings

# ============================================
# API Keys
# ============================================

# Groq API key for LLM (required)
# Get your key from: https://console.groq.com/keys
GROQ_API_KEY=gsk_your_groq_api_key_here


# ============================================
# TTS (Text-to-Speech) Configuration
# ============================================

# TTS Backend Selection
# Options:
#   - 'edge': Microsoft Edge TTS (cloud-based, low resource, network latency)
#   - 'sesame': Sesame CSM-1b (local GPU, high quality, low latency, requires CUDA)
# Default: edge
TTS_BACKEND=edge

# Sesame CSM-1b Settings (only used when TTS_BACKEND=sesame)
# Speaker ID for voice selection (0-based integer)
# Default: 0
SESAME_SPEAKER_ID=0


# ============================================
# Performance Notes
# ============================================

# Edge TTS:
#   - Pros: No GPU needed, good quality, free
#   - Cons: Network latency (200-500ms per chunk), requires internet
#   - Use when: CPU-only, limited VRAM, or testing

# Sesame CSM-1b:
#   - Pros: Ultra-low latency (~200-500ms total), offline, high quality
#   - Cons: Requires CUDA GPU (12GB+ VRAM recommended), larger model download
#   - Use when: Have GPU (RTX 4090, A100, etc.) and want minimal latency
#   - Expected RTF (Real-Time Factor): 0.28x on RTX 4090
#     (generates 10 seconds of audio in ~2.8 seconds)
